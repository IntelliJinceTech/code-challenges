// 2 parameters, both numbers
// return boolean
//
// ! given 2 parameters, base and factor
// !    return boolean based on whether base is divisible by factor

// !

function checkForFactor(base, factor) {
    return base % factor == 0;
}
