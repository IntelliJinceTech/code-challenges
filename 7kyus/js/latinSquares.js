// input - number, no floats, positive only, not including 0
// n sets of array of numbers,

function makeLatinSquares(n) {
	// code
	// initialize an empty array
	// LOOP from 1 to n  --- the number of elements in the array - the sets
	//      return array
	//      LOOP from 1 to n
	//
}

console.log(makeLatinSquares(4), [
	[1, 4, 3, 2],
	[4, 3, 2, 1],
	[1, 2, 3, 4],
	[2, 1, 4, 3],
])
