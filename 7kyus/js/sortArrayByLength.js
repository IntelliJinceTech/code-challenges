// array of strings - input
// returns a sorted array - ordered from shortest to longest, containing the same strings

function sortByLength(arr) {
	// Todo ITERATE over input array
	// Todo  initialize a variable
	// Todo     Map (?) or sort? - that would return a new array
	// todo     comparison of .length method of elements within the input array
	// todo return the new array

	return
}

console.log(sortByLength(['Beg', 'Life', 'I', 'To']))
